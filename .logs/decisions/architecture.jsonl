{"id":"architecture-001","date":"2026-05-31","decision":"VM command execution will be exposed as an optional nanofused API capability instead of direct CLI or tray calls into runtime tooling.","rationale":"The daemon owns VM lifecycle and runtime handles; keeping exec behind the daemon preserves the API boundary for macOS and Windows clients while allowing runtimes such as Apple container to implement command execution.","alternatives":"Directly shelling out to Apple container from the CLI was rejected because it bypasses nanofused and cannot work for remote API clients; implementing SSH as the primary control path was rejected because SSH depends on guest image contents and port forwarding rather than the runtime boundary itself.","references":[".claude/architecture.md",".flowspec/features/codex-goal/spec.md"]}
{"id":"architecture-002","date":"2026-05-31","decision":"Runtime capability gaps will use a shared unsupported-operation sentinel and API handlers will map it to HTTP 501 with UNSUPPORTED_OPERATION.","rationale":"Nanofuse now has Firecracker and Apple-container backends with different feature coverage; clients need to distinguish a valid operation unsupported by the selected runtime from daemon failures and guest command exit codes.","alternatives":"Mapping unsupported runtime features to INTERNAL_ERROR/500 was rejected because it misclassifies backend capability gaps as daemon failures; hiding unsupported operations only in clients was rejected because remote clients would still need server-side enforcement.","references":["docs/adr/adr-003-runtime-capability-and-api-error-boundary.md",".flowspec/features/codex-goal/design.md"]}
{"id":"architecture-003","date":"2026-06-01","decision":"VM operational inventory, daemon-allocated host ports, mounts, ingress, egress, and secret-use metadata belong in nanofused API objects rather than tray-local runtime inspection.","rationale":"Mac, Windows, and remote clients need the same authoritative VM state, and the daemon is the only process that knows the runtime host where ports are bound and security posture is enforced.","alternatives":"Tray-local runtime inspection was rejected because it bypasses the daemon API and cannot work for remote clients; free-form labels were rejected because VM management needs structured state-aware controls.","references":["docs/adr/adr-004-vm-inventory-and-exposure-metadata.md","api/openapi.yaml","internal/types/vm.go"]}
{"id":"architecture-004","date":"2026-06-01","decision":"Keep the current tray PR scoped to API and desktop-management usability while documenting Apple `container` as a macOS compatibility driver, not the final Nanofuse-owned Apple Virtualization.framework backend.","rationale":"The tray can be fixed independently from the lower runtime architecture. Claiming the Apple `container` path as the final macOS runtime would misstate the product target and make the PR harder to review.","alternatives":"Shipping the tray changes with final-runtime wording was rejected because it would encode the wrong architecture in docs. Blocking the tray PR on the native Apple VZ backend was rejected because the UI/API fixes are useful and validated separately.","references":["docs/GOALS.md","docs/TRAY_APP.md","docs/API_QUICK_START.md","scripts/run-tray-macos.sh"]}
