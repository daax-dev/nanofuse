# GOAL 

/goal we need to work thru every single github issue with using vagrant sandboxes (to isolate creation of a kvm / vm) as this is how you can get root in a ephemeral environment (and fully test) use the vagrant skill plugin
use subagents where possible.  do not stop until its all complete.
