image: ghcr.io/acme/agent:latest
config:
    vcpus: 4
    memory_mib: 2048
    network:
        mode: nat
        egress_policy:
            enabled: true
            default_action: drop
            allow_dns: true
